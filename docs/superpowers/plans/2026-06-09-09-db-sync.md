# DB 동기화 (Phase 2) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** 에디터의 현재 Project를 연결한 DB 구조로 동기화한다 — DBML 전체 교체 + 이름 기반 위치 보존 + 신규 테이블 빈공간 배치.

**Architecture:** 기존 `/api/introspect`(ADR-0008)와 `DbConnectDialog`(features/db-import)를 재사용. 신규 로직은 프론트엔드뿐: 순수 배치 유틸 `computeSyncedPositions`(entities/layout) + 에디터 페이지의 sync 오케스트레이션(덮어쓰기 확인) + TopBar 진입점. 결정: ADR-0009.

**Tech Stack:** React 19 · @xyflow/react · @dbml/core(parseDbml) · dagre(autoLayout) · 기존 autosave.

**커밋 트레일러:** 각 커밋 끝에 빈 줄 뒤 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

**테스트:** `docker compose -p erd-dbml exec -T frontend npm run test:run -- <path>` / 타입체크 `npx tsc -p tsconfig.app.json --noEmit`.

---

## File Structure
- Create: `frontend/src/entities/layout/lib/placeNewTables.ts` (+ test) — `computeSyncedPositions` 순수 유틸.
- Modify: `frontend/src/entities/layout/index.ts` — barrel export.
- Modify: `frontend/src/widgets/erd-topbar/ui/ErdTopBar.tsx` (+ test) — Sync 버튼 + `onSync` prop.
- Modify: `frontend/src/pages/editor/index.tsx` (+ test) — sync 상태/다이얼로그/확인/applySync.
- (선택) Create: `frontend/e2e/db-sync.spec.ts`.

---

## Task 1: `computeSyncedPositions` 배치 유틸

**Files:** Create `frontend/src/entities/layout/lib/placeNewTables.ts`; Test `frontend/src/entities/layout/lib/placeNewTables.test.ts`; Modify `frontend/src/entities/layout/index.ts`.

- [ ] **Step 1: 실패 테스트 작성** `placeNewTables.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { parseDbml } from '@/entities/dbml'
import { computeSyncedPositions } from './placeNewTables'

function schemaOf(dbml: string) {
  const r = parseDbml(dbml)
  if (!r.schema) throw new Error('parse failed: ' + JSON.stringify(r.errors))
  return r.schema
}

const A_B = `Table a {\n  id int [pk]\n}\nTable b {\n  id int [pk]\n}`
const A_ONLY = `Table a {\n  id int [pk]\n}`
const A_B_C = `Table a {\n  id int [pk]\n}\nTable b {\n  id int [pk]\n}\nTable c {\n  id int [pk]\n}`

it('keeps surviving table positions unchanged', () => {
  const current = { 'public.a': { x: 10, y: 20 }, 'public.b': { x: 300, y: 40 } }
  const out = computeSyncedPositions(current, schemaOf(A_B))
  expect(out['public.a']).toEqual({ x: 10, y: 20 })
  expect(out['public.b']).toEqual({ x: 300, y: 40 })
})

it('prunes removed tables from the result', () => {
  const current = { 'public.a': { x: 0, y: 0 }, 'public.b': { x: 300, y: 0 } }
  const out = computeSyncedPositions(current, schemaOf(A_ONLY)) // b removed
  expect(out['public.a']).toEqual({ x: 0, y: 0 })
  expect(out['public.b']).toBeUndefined()
})

it('places a new table below the existing bounding box, leaving existing positions intact', () => {
  const current = { 'public.a': { x: 0, y: 0 } } // a at top-left; b is new
  const out = computeSyncedPositions(current, schemaOf(A_B))
  expect(out['public.a']).toEqual({ x: 0, y: 0 }) // unchanged
  // a is 40 (header) + 1 row * 26 = 66 tall; new b must sit below + gap
  expect(out['public.b']).toBeDefined()
  expect(out['public.b'].y).toBeGreaterThanOrEqual(66 + 80)
})

it('returns empty when there are no existing positions (all new -> reconcile dagre handles it)', () => {
  const out = computeSyncedPositions({}, schemaOf(A_B))
  expect(out).toEqual({})
})

it('places multiple new tables (sub-layout) all below the existing box', () => {
  const current = { 'public.a': { x: 0, y: 0 } }
  const out = computeSyncedPositions(current, schemaOf(A_B_C))
  expect(out['public.b'].y).toBeGreaterThanOrEqual(66 + 80)
  expect(out['public.c'].y).toBeGreaterThanOrEqual(66 + 80)
})
```

- [ ] **Step 2: 실패 확인** `docker compose -p erd-dbml exec -T frontend npm run test:run -- src/entities/layout/lib/placeNewTables.test.ts` → FAIL(모듈 없음).

- [ ] **Step 3: 구현** `placeNewTables.ts`:
```ts
/**
 * PURE sync-layout helper (ADR-0009). Given the project's CURRENT stored
 * positions and the freshly-introspected schema, produce the FINAL layout for a
 * DB sync: surviving tables keep their coords, removed tables are pruned, and
 * new tables are packed into the empty band BELOW the surviving bounding box via
 * a compact dagre sub-layout. Persisting these positions means reconcile
 * (ADR-0004) honors them instead of dropping new nodes onto overlapping dagre
 * coords. Keys are node ids = `${schema}.${name}` (LayoutPositions convention).
 *
 * entities layer: imports entities/erd (schemaToFlow + autoLayout + nodeSize) and
 * the entities/dbml + entities/layout TYPES (mirrors reconcile's entity deps).
 */
import type { DbmlSchema } from '@/entities/dbml'
import { schemaToFlow, autoLayout, nodeSize } from '@/entities/erd'
import type { LayoutPositions } from '@/entities/layout/model/types'

const PLACEMENT_GAP = 80

export function computeSyncedPositions(
  current: LayoutPositions,
  schema: DbmlSchema,
): LayoutPositions {
  const { nodes, edges } = schemaToFlow(schema)
  const tableNodes = nodes.filter((n) => n.type === 'table')

  const surviving = tableNodes.filter((n) => current[n.id] !== undefined)
  const fresh = tableNodes.filter((n) => current[n.id] === undefined)

  // Surviving positions, pruned of removed tables (absolute coords only).
  const result: LayoutPositions = {}
  for (const n of surviving) {
    result[n.id] = { x: current[n.id].x, y: current[n.id].y }
  }

  if (fresh.length === 0) return result
  // Nothing to preserve → let reconcile's full-graph dagre lay everything out.
  if (surviving.length === 0) return {}

  // Bounding box of surviving nodes (stored coord + estimated rendered size).
  let minX = Infinity
  let maxY = -Infinity
  for (const n of surviving) {
    const { x, y } = current[n.id]
    const { height } = nodeSize(n)
    if (x < minX) minX = x
    if (y + height > maxY) maxY = y + height
  }

  // Compact sub-layout of the new tables (edges among new tables only).
  const freshIds = new Set(fresh.map((n) => n.id))
  const freshEdges = edges.filter(
    (e) => freshIds.has(e.source) && freshIds.has(e.target),
  )
  const placed = autoLayout(fresh, freshEdges)

  let subMinX = Infinity
  let subMinY = Infinity
  for (const n of placed) {
    if (n.position.x < subMinX) subMinX = n.position.x
    if (n.position.y < subMinY) subMinY = n.position.y
  }

  // Translate the sub-layout into the empty band below the surviving box.
  const targetX = minX
  const targetY = maxY + PLACEMENT_GAP
  for (const n of placed) {
    result[n.id] = {
      x: n.position.x - subMinX + targetX,
      y: n.position.y - subMinY + targetY,
    }
  }

  return result
}
```
Barrel: add to `frontend/src/entities/layout/index.ts`: `export { computeSyncedPositions } from './lib/placeNewTables'`.

- [ ] **Step 4: 통과 확인** 위 테스트 명령 → PASS(5).

- [ ] **Step 5: 커밋**
```bash
git add frontend/src/entities/layout/lib/placeNewTables.ts frontend/src/entities/layout/lib/placeNewTables.test.ts frontend/src/entities/layout/index.ts
git commit -m "feat(layout): computeSyncedPositions — preserve + place new tables for DB sync"
```

---

## Task 2: ErdTopBar — Sync 버튼

**Files:** Modify `frontend/src/widgets/erd-topbar/ui/ErdTopBar.tsx`; Test `frontend/src/widgets/erd-topbar/ui/ErdTopBar.test.tsx`.

- [ ] **Step 1: 실패 테스트 추가** (`ErdTopBar.test.tsx`에 append):
```tsx
it('renders a Sync from DB button and fires onSync', async () => {
  const user = userEvent.setup()
  const onSync = vi.fn()
  render(
    <ErdTopBar
      projectName="P"
      autosaveStatus="idle"
      onImportSql={vi.fn()}
      onBack={vi.fn()}
      onSync={onSync}
      exportMenu={<div />}
    />,
  )
  await user.click(screen.getByRole('button', { name: 'Sync from DB' }))
  expect(onSync).toHaveBeenCalledTimes(1)
})
```
(파일 상단 import에 `userEvent`/`vi`/`render`/`screen`가 이미 있으면 재사용. 없으면 기존 테스트 import 패턴을 따른다.)

- [ ] **Step 2: 실패 확인** `... npm run test:run -- src/widgets/erd-topbar` → FAIL.

- [ ] **Step 3: 구현** `ErdTopBar.tsx`:
  - `ErdTopBarProps`에 `onSync: () => void` 추가(필수).
  - lucide import에 `RefreshCw` 추가: `import { Info, Upload, ArrowLeft, RefreshCw } from 'lucide-react'`.
  - 함수 시그니처 구조분해에 `onSync` 추가.
  - Import SQL 버튼 바로 다음, Export 앞에 버튼 추가(secondary 스타일):
```tsx
        {/* Sync from DB button */}
        <button
          type="button"
          className="erd-topbar-btn"
          style={btnSecondary}
          onClick={onSync}
          aria-label="Sync from DB"
        >
          <RefreshCw size={15} strokeWidth={2} />
          Sync from DB
        </button>
```

- [ ] **Step 4: 통과 확인** → PASS.

- [ ] **Step 5: 커밋**
```bash
git add frontend/src/widgets/erd-topbar/ui/ErdTopBar.tsx frontend/src/widgets/erd-topbar/ui/ErdTopBar.test.tsx
git commit -m "feat(erd-topbar): Sync from DB button"
```

---

## Task 3: 에디터 페이지 — sync 오케스트레이션

**Files:** Modify `frontend/src/pages/editor/index.tsx`; Test `frontend/src/pages/editor/index.test.tsx`.

- [ ] **Step 1: 실패 테스트 추가** (`index.test.tsx`에 append — 기존 하네스/모킹 패턴을 따른다). 핵심 검증: Sync 버튼 클릭 → DbConnectDialog 열림; onIntrospected(dbml) 후 확인 다이얼로그 표시; 확인 클릭 시 에디터 DBML이 교체됨(`dbml-editor`의 값 또는 우측 패널 통계 변화로 확인). DbConnectDialog는 무거우므로 `@/features/db-import`를 모킹해 `DbConnectDialog`를 stub(열렸을 때 onIntrospected를 즉시 호출하는 버튼 노출)으로 대체:
```tsx
vi.mock('@/features/db-import', () => ({
  DbConnectDialog: ({ open, onIntrospected }: { open: boolean; onIntrospected: (d: string) => void }) =>
    open ? <button onClick={() => onIntrospected('Table synced {\n  id int [pk]\n}')}>fire-sync-introspected</button> : null,
}))
```
그리고: Sync 버튼 클릭 → "fire-sync-introspected" 클릭 → 확인 다이얼로그의 "Replace"(혹은 확인) 버튼 클릭 → 우측 패널 `stat-tables`가 `1`이고 `tablelist-row-synced`가 보이는지 단언(= DBML이 교체되어 재파싱됨). (테스트는 `useDbmlParse`를 모킹하지 말고 실제 파싱을 쓰되, 시드용 project.dbml_text를 비워두거나 1개 테이블로 둔다. 기존 editor 테스트의 project/useProject 모킹 패턴을 그대로 재사용.)

- [ ] **Step 2: 실패 확인** `... npm run test:run -- src/pages/editor` → FAIL(아직 sync UI 없음).

- [ ] **Step 3: 구현** `pages/editor/index.tsx`:
  - import 추가: `import { DbConnectDialog } from '@/features/db-import'`, `import { parseDbml } from '@/entities/dbml'`, `import { computeSyncedPositions } from '@/entities/layout'`, `shared/ui/dialog`의 `Dialog,DialogContent,DialogHeader,DialogTitle,DialogDescription`.
  - state: `const [syncOpen, setSyncOpen] = useState(false)` 와 `const [pendingSyncDbml, setPendingSyncDbml] = useState<string | null>(null)`.
  - `applySync`:
```tsx
  function applySync(dbml: string) {
    const parsed = parseDbml(dbml)
    if (parsed.schema) {
      setPositions(computeSyncedPositions(positions, parsed.schema))
    }
    setDbmlText(dbml)
    setPendingSyncDbml(null)
  }
```
  - `<ErdTopBar ... onSync={() => setSyncOpen(true)} />` 추가.
  - 하단 다이얼로그 영역(TableDocView/SqlImportDialog 옆)에:
```tsx
      <DbConnectDialog
        open={syncOpen}
        onOpenChange={setSyncOpen}
        onIntrospected={(dbml) => {
          setSyncOpen(false)
          setPendingSyncDbml(dbml)
        }}
      />
      <Dialog open={pendingSyncDbml !== null} onOpenChange={(o) => { if (!o) setPendingSyncDbml(null) }}>
        <DialogContent>
          <DialogHeader>
            <DialogTitle>Sync from database?</DialogTitle>
            <DialogDescription>
              This replaces the current DBML with the database's schema. Table
              positions are preserved and new tables are placed in empty space,
              but manual notes, table groups, and colors not in the database
              will be lost.
            </DialogDescription>
          </DialogHeader>
          <div className="flex justify-end gap-2">
            <Button variant="outline" onClick={() => setPendingSyncDbml(null)}>Cancel</Button>
            <Button onClick={() => { if (pendingSyncDbml) applySync(pendingSyncDbml) }}>Replace</Button>
          </div>
        </DialogContent>
      </Dialog>
```
  - `DbConnectDialog`의 `onIntrospected`는 `(dbml, databaseName)` 시그니처지만 두 번째 인자는 무시(sync는 이름 불필요).

- [ ] **Step 4: 통과 확인** `... npm run test:run -- src/pages/editor` → PASS. 그리고 전체 `... npm run test:run` green + `npx tsc -p tsconfig.app.json --noEmit` clean.

- [ ] **Step 5: 커밋**
```bash
git add frontend/src/pages/editor/index.tsx frontend/src/pages/editor/index.test.tsx
git commit -m "feat(editor): Sync from DB — overwrite confirm + name-based layout preservation + new-table placement"
```

---

## Task 4: E2E + 최종 회귀 + 문서 커밋

**Files:** (선택) Create `frontend/e2e/db-sync.spec.ts`; commit docs.

- [ ] **Step 1: (선택) E2E** `frontend/e2e/db-sync.spec.ts`(portable, 기본 baseURL — sibling spec 패턴): 로그인 → API로 1개 테이블 DBML 프로젝트 생성(`a` 테이블) + 위치를 가진 layout → 에디터 열기 → "Sync from DB" → DbConnectDialog에 스택 자체 Postgres 좌표(host `postgres`/5432/`erddbml_user`/`postgres_dev`/`erddbml_dev`/`public`) → Connect → 확인 "Replace" → ERD가 스택 스키마(`user`,`project` 등)로 갱신되고 `erd-canvas` 표시. (로컬 4001 override로 인해 green 실행은 db-import 스펙과 동일 제약 — 셀렉터/컴파일만 확인하고, 필요 시 임시 4001로 검증.)
- [ ] **Step 2: 최종 회귀** `docker compose -p erd-dbml exec -T frontend npm run test:run`(green) + `npx tsc ...`(clean) + 백엔드 `pytest -q`(무영향 확인).
- [ ] **Step 3: 문서/커밋** ADR-0009·스펙·플랜·CONTEXT.md 변경을 커밋:
```bash
git add docs/adr/0009-db-sync-replace-and-place.md docs/superpowers/specs/2026-06-09-db-sync-design.md docs/superpowers/plans/2026-06-09-09-db-sync.md CONTEXT.md frontend/e2e/db-sync.spec.ts
git commit -m "docs(db-sync): ADR-0009 + spec + plan + CONTEXT term; e2e"
```

---

## Self-Review
- **Spec coverage:** 전체 교체(applySync setDbmlText) ✓, 이름 기반 보존(computeSyncedPositions surviving) ✓, 삭제 제거(pruned) ✓, 신규 빈공간 배치(sub-layout below bbox) ✓, 진입점+덮어쓰기 확인(TopBar+Dialog) ✓, 백엔드 무변경 ✓. Phase 2 범위 전부 매핑.
- **Type consistency:** `computeSyncedPositions(current: LayoutPositions, schema: DbmlSchema): LayoutPositions`; LayoutPositions 키=`${schema}.${name}`(schemaToFlow node id와 일치); DbConnectDialog `onIntrospected(dbml, dbName)` 재사용(dbName 무시). autosave가 setDbmlText/setPositions 결과를 영속화.
- **Placeholder scan:** 모든 스텝에 실제 코드. E2E 좌표는 db-import에서 검증된 실측값.
